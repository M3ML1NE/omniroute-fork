#!/usr/bin/env node
/**
 * Post-install hook placeholder.
 *
 * The native-runtime warm-up that previously lived here depended on the CLI
 * runtime supervisor, which has been removed. Kept as a no-op so the
 * `scripts/build/postinstall.mjs` import chain stays intact.
 */

process.exit(0);
