// The proxy registry routes and proxy logger were intentionally removed from the
// GigaChat fork. This file remains as a tombstone so old ledgers can point to
// the deleted integration surface without importing removed modules.
