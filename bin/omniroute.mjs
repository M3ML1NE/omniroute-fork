#!/usr/bin/env node\n\n/**\n * OmniRoute Thin Entry Point\n * Used only to boot the Next.js server.\n * CLI and MCP capabilities have been removed.\n */\n\nimport { join, dirname } from "node:path";\nimport { fileURLToPath } from "node:url";\n\nconst __filename = fileURLToPath(import.meta.url);\nconst __dirname = dirname(__filename);\nconst ROOT = join(__dirname, "..");\n\nconsole.log("OmniRoute server boot...");\n\n// Just start the Next.js production server\nimport("next/dist/bin/next");
